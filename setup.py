from setuptools import setup, find_packages

setup(name='Resolve-comments',
      version='0.0.1',
      description='Resolve comments faster',
      author='',
      author_email='',
      url='',
      packages=find_packages(),
     )